# PostgreSQL connection string (optional for local UI; required for db ping / later schema work)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/aa_leave_tracker

# Better Auth (used in a later PR — do not set a real secret in git)
# BETTER_AUTH_SECRET=
